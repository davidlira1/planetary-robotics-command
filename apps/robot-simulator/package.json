{
  "name": "@prc/robot-simulator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "start:dev": "pnpm --filter @prc/simulation build && ts-node --transpile-only src/main.ts",
    "test": "jest --config ./jest.config.js"
  },
  "dependencies": {
    "@prc/http-telemetry-producer": "workspace:*",
    "@prc/ports": "workspace:*",
    "@prc/simulation": "workspace:*",
    "dotenv": "^16.4.7",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
