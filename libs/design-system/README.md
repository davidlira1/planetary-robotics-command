# @prc/design-system

Framework-neutral Deep Space tokens for Planetary Robotics Command.

Import `tokens.css` for CSS custom properties and `@prc/design-system/tokens` for the matching JS color map. Do not scatter palette hex values in feature components or Three.js files.

See ADR 0019.
